server:
  port: 8769

zuul:
  routes:
    api-auth:
      path: /auth/**
      serviceId: auth
    api-payment:
      path: /payment/**
      serviceId: payment
    api-uc:
      path: /uc/**
      serviceId: uc